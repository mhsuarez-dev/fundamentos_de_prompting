{
  "name": "simulador-fundamentos-prompting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p public && cp index.html public/index.html && (cp firebase-applet-config.json public/ || true)",
    "lint": "echo 'No lint step required'"
  },
  "dependencies": {
    "@google/genai": "latest",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
